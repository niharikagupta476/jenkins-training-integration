@Library('scripted-demo@scripted-pipeline', 'additional_library1')_
demo{
    dummy_var = 'dummy_var_value'
    //This is also a required line, cannot be deleted
    //this is required line, cannot be deleted
}
